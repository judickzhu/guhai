# DCOGAI 開發轉達稿：後台知識庫文本亂碼 Bug（U+FFFD 替換符，P0）

> 日期：2026-09-05
> 問題：s.dcogai.com 後台知識庫部分條目在**存儲/返回**時出現亂碼（U+FFFD �），本地數據完全正確，重傳無效——判定為**後台服務端數據層 bug**（與歷史記載的 DELETE 全局 id bug 同源，屬後台對長文本/特定字符處理不可靠）。
> 影響：線上客服回答含「�」，用戶可見。

---

## 一、現象

後台 `GET /api/admin/kb` 返回的部分 `a`（答案）含 **U+FFFD 替換符（�）**，且**條目長度比正確文本多 2 字節**——即後台在某個位置插入/替換了半個 UTF-8 中文字。

**數量**：約 50 條（分散於 9+ 個分類），兩大書分類（股路不歸 23 條、股道 11 條）最集中。

**鐵證——同文本反覆 POST 仍壞**：收費授權「為什麼會試用一年？」連續重建 3 次（刪分類→建分類→POST 本地正確 UTF-8 文本），每次 GET 回來仍在同一位置亂碼。→ 不是網站側傳輸/編碼問題，是後台存儲後即損壞。

---

## 二、復現證據（本地正確 vs 後台亂碼）

| 分類 | 問題 | 後台長度 | 本地長度 | 後台文本(片段) | 本地正確文本(片段) |
|---|---|---|---|---|---|
| 收费授权 | 為什麼會試用一年？ | 227 | 225 | 「…覺得行，留下；不**��**，走…」 | 「…覺得行，留下；不**行**，走…」 |
| 情绪识别 | 为什么我明明看对了方向 | — | — | 「…記住，交易不**���**…」 | 「…記住，交易不**獎勵最會**…」 |
| 反向思维训练 | 連跌三天了 | — | — | 「…出了什麼**���**市場也…」 | 「…出了什麼。市場也是…」 |
| 冲突场景 | 你是個傻鳥！ | — | — | 「…正面回答你**��**它的價…」 | 「…正面回答你：它的價…」 |
| 认知节点 | 我只关心一件事 | — | — | 「…當核**��**賣點」 | 「…當核心賣點」 |
| 認知節點地圖 | 為什麼別人一天做十幾單 | — | — | 「…有**���**次只是為…」 | 「…有八十次只是為…」 |
| 主干链05 | 我最近連續賺了十幾單 | — | — | 「…一件事。**��**續賺錢…」 | 「…一件事。連續賺錢…」 |
| 股路不歸·DC問答 | 你的恐懼是你的警報器… | — | — | 「…恐懼**��**貨，行情…」 | 「…恐懼出貨，行情…」 |
| 股道·DC問答 | 貪小便宜…一夜暴富 | — | — | （同模式） | （同模式） |

> 完整 50 條證據：`/tmp/corrupt_evidence.json`（每條含 分類/問題/後台長度/本地長度/首個亂碼位置/雙側上下文）。

---

## 三、規律分析

1. **每處多 2 字節**：後台長度 = 本地長度 + 2（每處 � 取代 1 個正確中文字 → 原 3 字節變 2 字節 �？不，實測是 +2——疑似後台把某 3 字節字拆成「1 字節殘片 + �」）。
2. **壞字不固定**：行/會/連/的/心/次/錯/麼/出/人/望/果 都出現過 → 非特定字觸發。
3. **壞位不固定**：有的在開頭（pos9），有的在中段（pos209），有的在結尾——非固定偏移。
4. **與長度相關**：多發生在 150-230 字節左右及以上的中長文本；短文本（<60 字節）未見。
5. **emoji 相關性**：含 😊/～ 的條目約 70% 出問題，但非必然（大量含 😊 條目正常）→ 疑似**多字節字符序列處理在特定邊界出錯**（代理對/組合字符/斷字邊界）。

---

## 四、建議後端排查方向（對照既有需求① DELETE bug 同源）

1. **存儲層編碼**：POST 收到的 `a` 是否原樣入庫？是否在 DB 層做了錯誤的字符集轉換（如 utf8mb4 截斷 / 誤用 latin1 / 代理對拆分）？
2. **長度截斷**：是否有按「字節數」截斷的邏輯（如 VARCHAR/欄位上限按字節算），在中文字符邊界切出半個字 → 補 �？
3. **返回層**：GET 序列化時是否對部分字符做了二次編碼？
4. **復現建議**：用一條已知壞題（如「為什麼會試用一年？」本地 a 225 字節）直接 POST → GET 對比；若 POST 後立即可見亂碼，則為入庫即壞；若重啟/刷新後才壞，則為讀取路徑問題。

**網站側已在等**：本地知識庫已全部繁體化＋禁語清理完畢（34 類/2891 題，驗證 0 亂碼 0 禁語），guhai 已同步；待後端修復此亂碼 bug 後，網站側可再全量同步一次，即可完全一致。

---

## 五、附：本次同步歷史（供後端對照）

- 2026-09-05：全庫繁體化（opencc s2t，354 條 a 轉換）＋34 處禁語清理＋34 條 a_en 重譯。
- 網站側用「刪分類→重建分類」分批同步（`rebuild_cats.js`，純 Node https 直連 admin API），因 QA 級 DELETE/PUT 有全局 id bug（見 `DCOGAI開發轉達稿-刪除接口.md`）不可用。
- 分類級 DELETE 偶發 socket hang up（返回前連接斷，但實際可能已刪）——需以 GET 復查為準。
