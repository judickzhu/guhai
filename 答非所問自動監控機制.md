# 《答非所問自動監控機制》（V3.4）

## 為什麼需要
之前的答非所問（比特幣歸零→離鄉題、來這裡目的→硬碟題、聽懂沒必要→漲幅題）都是**人工發現**。需要自動化。

## 三層監控

### A 層：運行時防護（主動防，已上線 v53）
1. **書籍兜底門檻 4.0**：3.x 分的書籍題命中是 n-gram 噪音搶答（如「離鄉」題被「歸零了嗎」命中 3.5 分）→ 直接不答，避免答非所問
2. **弱命中標記**：非書籍分數 <4 → 標記 weak
3. **弱命中提示**：回答附註「姐姐這個回答可能沒完全對上你的問題，你可以再問得具體一點」——用戶知道可追問

### B 層：問答日誌（被動偵測，已上線 v53）
網站記錄每條本地匹配命中到 `localStorage.qa_log`（上限 200 條）：
```json
{"ts":时间戳, "q":查詢, "cat":命中分類, "hit":命中題目, "score":分數, "weak":是否弱命中, "book":是否書籍兜底}
```

### C 層：監控分析（自動揪出，已上線）
```bash
node kb_iterate.js --monitor --file=qa_log.json
```
- ①弱命中（<4 分）→ 疑似答非所問
- ②書籍兜底 → 需人工確認
- ③重複提問（同問題 ≥2 次）→ 用戶可能沒得到答案
- ④低分命中（4-5 分）→ 潛在改進點

## 使用流程
```
1. 用戶在網站聊天（產生 qa_log）
2. 用 ego-browser 導出 localStorage qa_log → qa_log.json
3. node kb_iterate.js --monitor --file=qa_log.json
4. 依清單修復（補題/補keywords/停用詞）→ 加入語料庫防回歸
```

## 監控信號優先級
| 信號 | 置信度 | 處理 |
|---|---|---|
| 弱命中 + 書籍兜底 | 高 | 補題或加停用詞 |
| 弱命中（非書籍） | 中 | 查 keywords 缺口 |
| 重複提問 | 中 | 用戶沒得到答案 → 語義檢查 |
| 低分命中 | 低 | 潛在改進點 |
