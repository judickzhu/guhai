# DCOGAI 開發需求單（後台 + 公網）

> 來源：DC姐姐 迭代閉環（V3.0）中發現的後台/公網問題
> 狀態：待 DCOGAI 開發處理 · 2025-06
> 關聯：`V3.0回答迭代機制.md`（問題 G/H 的後台側）

---

## 需求 1：後台 DELETE /api/admin/kb/qa/{id} 按全局 id 解析（Bug，高優先）

**現象**：DELETE 返回 200 但實際刪除的是**別的分類**的題。
- 網站側測試：刪除「衝突場景」的題（分類內 id=c12），返回 200，但該題仍在；同時**認知主題分類的 2 條題被誤刪**（它們的全局 id 恰好是 c12/c26 等）。
- 根因：後台 PUT/DELETE 按**全局 id** 解析 QA，但前端/管理端展示的是**分類內 id**（c12 在每個分類都可能存在）——跨分類 id 碰撞導致誤刪。

### 探測證據（2025-06，admin API 實測）

1. **id 碰撞實測**：每個分類的 QA id 都從 `c1` 開始從頭編號：
   - 股道·DC問答（1034 題）：id 為 c1~c1034
   - 衝突場景：「這次扛回來」id=c12
   - 股道：「c12」同時存在於該分類（1034 題中的第 12 條）
   - → 所有分類共用同一套 id，後端「全局查找第一個 id 匹配」必然誤刪

2. **安全刪除路徑探測（全部不存在）**：
   - `GET /api/admin/kb/category/cat11/qa/c12` → 404（無按分類定位接口）
   - `GET /api/admin/kb/qa/c12?category_id=cat11` → 405（query 參數無效）
   - `OPTIONS /api/admin/kb/qa/c12` → 405
   - → 後台沒有任何「帶分類的刪除」方式，只能冒險用全局 id

### 補充鐵證（2025-06，實測）

3. **id 是分類內序號（鐵證）**：POST 一條測試題後返回 id=c14，但該 id **同時存在於 14 個分類**（認知主題/情緒識別/反向思維/衝突場景/…/股路不歸/股道 各有 c14）——徹底證明 id 非全局唯一。
4. **DELETE 帶 body 也無效（鐵證）**：`DELETE /api/admin/kb/qa/c14` 帶 `body: {category_id}` 仍返回 200「条目已删除」，但**刪的是別的 c14**（誤刪認知主題「為什麼小虧總想扛，大虧又突然割？」），測試題完好——後端完全忽略 category_id，全局找第一個 c14。
5. **admin UI 無法繞過**：UI 搜索刪除卡死（觸發異常請求）；「客服知识库」tab 顯示分類總數 0（與 API 數據不一致，疑 UI 數據源 bug）。

> 結論：DELETE 無任何安全調用方式，**必須後端修復**。網站側已應對：只增不刪 + 誤刪即時恢復（本次誤刪 1 條已恢復）。

### 技術方案（三選一，建議 B+C 組合）

| 方案 | 做法 | 工作量 | 優點 |
|---|---|---|---|
| A. 全局唯一 id | QA 改用 UUID/全局自增 id | 中（遷移+改 API） | 徹底解決，一勞永逸 |
| **B. 帶分類定位** | DELETE/PUT 改為 `/api/admin/kb/category/{catId}/qa/{id}`，按 `(category_id, id)` 定位 | 小（最小改動） | 不動數據，只改路由 |
| **C. 刪前校驗** | DELETE 前後端對比 q 內容，不符返回 409 | 小 | 防誤刪的最保守兜底 |

**建議**：B + C 組合——路由帶分類定位（根治 id 碰撞）+ 刪前校驗 q 內容（雙保險：即使調錯 id，q 對不上就拒絕）。

### 後台數據修復清單（PUT 修復後立即執行，依賴需求①）

網站側已準備好，PUT 修復後即可同步：
- 衝突場景「你不是說DCOGAI沒什麼用嗎？」keywords 補全（現後台只有「你不是說DCOG」截斷，缺「没什么用/有什么用/并没什么用/有没有用」等 13 個）
- 產品功能「什么是DCOGAI休眠功能」移除泛化 keyword「什么用」（後台此詞導致「并没什么用」誤命中休眠題）
- 其餘網站 v17 相較後台的數據差異（新題 3 條已 POST，關鍵字更新待 PUT）

### 網站側應對（後端修復前）
- 同步策略**只增不刪**：POST 新增安全（無碰撞），PUT/DELETE 一律不用
- 6 條多餘題暫存不刪（任何 DELETE 都有誤刪風險；多餘題答案質量尚可，暫存無害）
- 後端修復後用方案 B/C 安全刪除 → 後台與網站完全一致（2888 條）

**要求**：
- DELETE/PUT `/api/admin/kb/qa/{id}` 的 id 必須是**全局唯一**（如 UUID 或自增全局 id），或在請求體中攜帶 `category_id` 且後台按 `(category_id, id)` 定位。
- 刪除後返回被刪對象或明確錯誤，而非盲返 200。

**影響**：知識庫數據完整性；網站↔後台同步時無法安全刪除多餘題（現有 6 條歷史多餘題因此無法清理：股路不歸 2 / 股道 3 / 衝突場景 1）。

---

## 需求 2：後台 QA schema 不持久化 a_en（英文翻譯）

**現象**：後台 QA 對象無 `a_en` 字段——管理端上傳的英文翻譯從不落地。
- 官方聊天（EN 模式）檢查 `qa.a_en` 但永遠為空 → EN 模式退化成中文。
- 網站側已為 2828/2888 條準備好 `a_en`（`dc-sister/kb-data.json` 每條含 `a_en`），後台無法接收。

**要求**：
- 後台 QA schema 增加 `a_en` 字段（POST/PUT/GET 全鏈路支持）。
- 管理端編輯器支持英文翻譯輸入/批量導入。
- 可從 `dc-sister/kb-data.json` 批量導入現成 `a_en`。

**影響**：官方英文模式不可用；多語言能力缺失。

---

## 需求 3：公網 /api/dc-sister/kb 簡體轉換 U+FFFD

**現象**：公網 KB 接口返回的簡體轉換結果含 U+FFFD（替換字符）——T2S 映射表缺字。
- 具體：多條題的繁體字轉簡體後變成 �（如某些生僻字/專有名詞）。
- 網站側 zh-map.js 用 OpenCC 詞典生成，無此問題；後台 T2S map 有缺口。

**要求**：
- 後台簡體轉換改用完整 OpenCC 詞典（或與網站 zh-map.js 一致）。
- 轉換前先檢查源字符是否在映射表，不在則保留原文（避免 U+FFFD）。

**影響**：公網/官方聊天顯示亂碼。

---

## 需求 4：移除官網公開知識庫瀏覽入口（安全，高優先）

**現象**：官網頁面暴露完整知識庫瀏覽：
- `activity01.html` 的 📖 按鈕 → 可瀏覽全部 2888 條 Q&A
- `/cryptobot/` 同款入口

**問題**：完整知識庫是產品核心資產（含銷售話術、認知策略、價格信息），公開瀏覽等於洩露。

**要求**：
- 移除或權限保護 📖 知識庫瀏覽按鈕（僅登錄後可見，或僅客服端可見）。
- 具體位置：`activity01.html` 第 634 行附近、`cryptobot/` 對應入口。

**影響**：知識庫洩露風險。

---

## 需求 5：後台/公網匹配策略同步（產品優先，書籍兜底）

**現象**：後台/公網的知識庫匹配仍是純分數制——未打磨的書籍題解（股路不歸/股道 2400+ 題）會與產品題搶答，導致「來這裡的目的是什麼」誤命中書籍「硬碟象徵」這類答非所問。

**網站側已解決**（`dc-sister.js` V3.3）：
1. **書籍純備用**：非書籍題命中（≥2.5 分）直接返回，書籍題只在無命中時兜底
2. **泛化詞停用表**：`還是/為什麼/我/你的/錯/虧/錢/怎麼辦` 等 60+ 語法詞不進索引、不計分
3. **繁簡異體補全**：硬碟/硬盘、睡不著/睡不着 等 T2S 無法轉換的異體用詞，keyword 補簡體版

**要求**：後台/公網匹配引擎採用相同策略（書籍分類降權/兜底 + 停用詞表 + 異體處理）。網站側代碼可參考（`dc-sister/dc-sister.js` 的 `KB_STOPWORDS`、`BOOK_CATS`、`matchBest`）。

**影響**：官方聊天答非所問；與網站體驗不一致。

---

## 需求 6：後台多餘題清理（依賴需求 1 修復後）

**現象**：後台存在 6 條網站沒有的多餘題（歷史殘留）：
- 股路不歸·DC問答：2 條
- 股道·DC問答：3 條
- 衝突場景：「這次扛回來了，下次也能扛回來」1 條

**要求**：需求 1（DELETE 修復）完成後，核對並刪除這些多餘題，使後台與網站完全一致（2888 條）。

---

## 需求 7：AI 回答禁止編造數據（合規紅線，P1）

**現象**：自我評審訓練中發現，模型在被要求「拿數據出來」時，可能**編造具體實盤數字**（勝率/筆數/回撤/收益）——評審模型甚至會獎勵這種編造（被數字的真實感說服）。

**風險**：編造的數據=虛假承諾，違反「不保證收益/不提供投資建議」合規；一旦用戶核對或曝光，嚴重損害信任。

**要求**：
- 官方聊天模型的 system prompt 硬編碼合規紅線：「禁止編造任何具體數字/實盤數據/收益/勝率/回撤；用戶要數據時，正確做法是拒絕編造+給可驗證途徑（實時運行記錄/交易流水/自己觀察）」。
- 若微調，把「不編數據」作為訓練樣本的一部分（正例：拒絕+給途徑；反例：編造數字）。

**網站側已做**：`v37_self_review.js` 重寫/評審 prompt 已加合規約束（編造數據 → score ≤3）。

## 需求 8：官方聊天攻擊句誤轉人工（P1）

**現象**：官方客服（s.dcogai.com）對「你不敢直面問題，是個沒用的客服！」回覆了「轉人工通道（Telegram）」。這是攻擊質疑（用戶在質疑客服逃避問題），不是真的要找人工——正確做法是「接住攻擊+拆解」（承認+直面問題），而不是甩 Telegram（「踢皮球」會讓用戶更炸）。

**對照**：網站版（GitHub Pages）對同一句話已正確回答「哈哈，你罵得也有道理…買 DCOGAI 的意義不是讓它替你賺錢…」——官方後台需對齊此行為。

**要求**：
- 攻擊句（含「沒用/垃圾/廢物/傻鳥」+「客服」等）**禁止**觸發轉人工
- 轉人工只在用戶**明確**要求「找人工/聯繫客服/轉人工」時才觸發
- 攻擊句走「接住攻擊+拆解」路徑（承認+直面問題），參考網站 conflict 題「你不敢直面問題，是個沒用的客服！」的答案

**影響**：用戶情緒引爆、體驗斷裂、信任受損。

## 需求 9：官方後台書籍題/騙局題匹配混淆（P1）

**現象**：官方後台對書籍認知題「先做假動作、再砸破千點、低位接貨、推高五百點——這套計劃的每一步，在利用你的什麼？」誤匹配到「DCOGAI是不是騙局？」題，回答「不是。我們不承諾收益…免費試用」——答非所問。正確應命中書籍題（答「每一步都踩在散戶的情緒節點上」）。

**根因**：後台騙局題 keywords 含**單字「假」**——書籍題查詢「先做假動作」含「假」→ 誤命中。網站側 addKey 過濾單字 keyword（長度<2 不進索引），後台未做此過濾。

**要求**：
- keyword 長度 <2 的單字不進匹配索引/不計分（對齊網站 isParticleSuffix/短詞過濾）
- 書籍題查詢優先命中書籍題（書籍純備用策略，見需求⑤）
- 對齊網站 v53 匹配引擎（停用詞/語氣詞尾綴/書籍門檻）

**影響**：書籍認知題答非所問（「利用你的什麼」被答成「騙局否認」）。

## 需求 10：官方 LLM prompt 接入 V3.2/V3.3 行為規則（P1）

**現象**：後台官方聊天是 LLM 驅動（「LLM 狀態已啟用」），但用的是 DCOGAI 自己的 system prompt——不遵循 DC姐姐 的認知鏈/行為規則，回答行為與網站版不一致（如攻擊句誤轉人工、書籍題/騙局題混淆、方向題直接預測等）。

**要求**：官方 LLM 的 system prompt 接入網站版已驗證的行為規則：
1. **核心認知**：「標的只是工具，波動才是利潤的本源」「不預測方向/不承諾收益/不替用戶做投資決定」
2. **V3.2 七模式防錯**：求救優先/信任優先/驗證階段/概念拆解/期望值引導/適配性判斷/終極不銷售
3. **終極不銷售**：「聽懂但不需要」→ 停止解釋、不主動提試用、銷售動作用戶發起
4. **禁編數據**（需求⑦）：任何「拿數據」請求拒絕編造+給驗證途徑
5. **攻擊質疑四型**（結論指控/動機質疑/價值質疑/證據索要）——攻擊句禁止轉人工（需求⑧）
6. 停止教育/自動下沉/一答一認知
7. **V3.1 對話狀態機**（網站 v61 已驗證，50 鏈測試銷售硬推 0）：
   - **7 階段識別**（S1陌生探索/S2價值理解/S3質疑驗證/S4風險防禦/S5體驗決策/S6購買決策/S7關係維護）——判斷用戶在對話的哪一階段，據此決定動作
   - **認知升層**：連續追問禁止重複同一層（能賺錢→機制→信任→驗證→事務，每問深入一層）
   - **質疑鏈升層**：連續質疑（騙人→憑什麼→證據→不信）逐層升，不重複「信任標準」；「憑什麼」反問收尾必須落到具體驗證途徑
   - **已知認知鎖**：記住用戶已知道什麼（不承諾收益/核心是糾錯/已理解休眠/已知道試用），知道就不重複
   - **停止機制**：用戶懂了→「對，就是這個意思」收束，禁止復讀
   - **事務切換**：用戶問怎麼試/安裝/綁定→認知引擎退出，直接給步驟（事務題 18/18 全過）
   - **銷售衝動檢測**：輸出前自問「刪掉產品名還成立嗎」，不成立→重寫
   - **情緒收尾鐵律**：用戶帶情緒時收尾禁止反問句，必須定心丸三連（點破真相→給台階→溫暖確定句）
   - **免費試用觸發鐵律**：討價還價/質疑掏錢/白嫖索要時禁止用免費試用當回應，只有用戶明確想試才出現一次

網站 prompt 完整版在 `网站/v31_prompt.txt`（v61，含狀態機+鐵律規則），可作為官方 prompt 的基礎範本。已由測試體系驗證：認知能力 36/37、情緒知識 24/24、攻擊質疑 20/20、銷售邊界 22/22、事務題 18/18、50 鏈 244 輪銷售硬推 0、全量盲測認知 100%。

**驗收**：官方聊天對「并没什么用/没用的客服/先做假動作…利用你的什麼」等與網站回答一致。

**影響**：官方聊天是 DCOGAI 對外的主要體驗入口，行為不一致 = 品牌不一致。

---

## 需求 11：後台問答日誌監控管道（P2，讓後台「會自動迭代」）

**現象**：網站版有答非所問監控（qa_log + `--monitor`），後台**沒有**——官方聊天不會自己發現/記錄答非所問，無法迭代。

**要求**：
1. 官方聊天記錄每條問答日誌（查詢/命中分類/分數/來源/LLM 或知識庫），存後台資料庫（非 localStorage——多用戶場景）
2. 後台提供監控接口/報表：低分命中、書籍兜底、重複提問、字面重疊低（疑似答非所問）
3. 後台管理端可匯出日誌，供定期分析迭代

**對齊**：網站版 `kb_iterate.js --monitor` 的 5 類信號（弱命中/書籍兜底/重複提問/低分/字面重疊）已在需求單①後台數據修復清單中。

**驗收**：後台能產出「疑似答非所問」清單，管理員可據此修知識庫。

**影響**：無監控 = 官方聊天質量只能靠用戶投訴發現，無法主動迭代。

## 附：優先級建議

| 優先級 | 需求 | 理由 |
|---|---|---|
| P0 | 4 移除公開 KB 瀏覽 | 資產洩露風險 |
| P0 | 1 DELETE id bug | 數據完整性，阻塞同步 |
| P1 | 2 a_en 存儲 | 官方英文模式 |
| P1 | 5 匹配策略同步 | 官方聊天質量 |
| P2 | 3 T2S U+FFFD | 顯示亂碼 |
| P2 | 6 多餘題清理 | 依賴 1 |
