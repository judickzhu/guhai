# DCOGAI 後台知識庫全量更新記錄（2026-09-13）

## 完成的操作（按用戶指示：下載→全量更新→恢復）

1. **下載當前版**：後台 UI「下载当前版」→ /tmp/kb_current_backend.json（34 分類/2889 題，q_en=0）
2. **合併 q_en**：從 GitHub repo（kb-data.json 含 487 q_en）+ zh-map 繁簡轉換（474 匹配）+ LLM 補譯 13 題 → 487/487
3. **上傳全量文件**：`/api/admin/kb/upload-full`（multipart FormData）→ 「客服知识库已全量更新」
4. **驗證**：34 分類/2889 題、非書籍 q_en 487/487、書籍 q_en 0

## 後台最終狀態

| 項 | 值 |
|---|---|
| 分類 | 34（全簡體）|
| 總題 | 2889 |
| 非書籍 q_en | **487/487（100%）** |
| 書籍 q_en | 0（按用戶要求跳過）|
| 版本 | 2026-09-13 16:31 UTC（2.18MB）|

## 使用的後台功能（後台 UI 內建）

- ✅ 備份當前版（歷史版本 2+）
- ✅ 下載當前版
- ✅ **上傳全量文件**（`/api/admin/kb/upload-full`，multipart）
- ✅ 恢復（歷史版本可恢復，`/api/admin/kb/restore` 需 versionId）

## 教訓

1. 後台 id 是分類內序號（跨分類同名）——匹配必須按 q 內容，不能按 id
2. 後台 qa PUT 不支持 q_en 字段（數據模型無此字段）——**必須用「上傳全量文件」方式**（後台 UI 功能）
3. zh-map 是 `window.DCSisterScript`（非 ZH_MAP）——node 需模擬 window 執行
4. 上傳接口是 multipart（FormData file 字段），非 JSON body
