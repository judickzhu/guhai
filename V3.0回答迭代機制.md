# 《DC姐姐 V3.0 回答迭代機制》

> 目的：把散落的「發現問題→歸因→修復→回歸」循環，正式化為**可重複執行的一條命令**。
> 解決「有沒有迭代機制」——有循環，現在補上機制化的管道。
> 版本：V3.0 · 2025-06
> 狀態：✅ 已落地（工作台 + 語料庫 133 條 + 首次閉環驗證）
> 閉環進度：第一批 24 問題→1（抽查 132/133），後台同步需補 0（僅 1 條歷史多餘待 DCOGAI 刪除）

---

## 一、為什麼需要機制化

過去 8 個迭代工具各自為政（盲測/歸因/專項優化/鏈驗證/抽查/停用詞/後台同步），靠人工觸發、靠記憶串聯。問題：
- 改了知識庫不知道影響了哪些匹配
- 新增題不知道該補什麼 keywords
- 誤命中靠用戶發現而不是主動防
- 沒有一處把「問題→修法」沉澱成可復用的知識

**機制化目標**：一次命令，跑完「掃描風險→抽查質量→出問題清單→給修復建議」，人只需要確認和執行。

---

## 二、閉環總覽

```
┌─────────────────────────────────────────────┐
│  ① 語料庫 kb_queries.jsonl                  │
│    （100盲測題 + 50抽查句 + 真實用戶話術）     │
└──────────────────┬──────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│  ② 迭代工作台 kb_iterate.js（一條命令）       │
│    a. 掃描：泛化 keyword / 繁簡異體缺口        │
│    b. 抽查：語料全量跑 matchBest 分類合理性     │
│    c. 歸因：誤命中/缺題/keywords缺口/異體      │
│    d. 出問題清單 + 修復建議                   │
└──────────────────┬──────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│  ③ 修復（每類有標準修法）                     │
│    補題 / 補keywords / 停用詞 / prompt規則    │
└──────────────────┬──────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│  ④ 回歸驗證（全過才上線）                     │
│    語料全量通過 + KPI（盲測）不退化            │
└──────────────────┬──────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│  ⑤ 版本 bump + 後台同步                      │
└─────────────────────────────────────────────┘
```

---

## 三、問題分類與標準修法（核心知識庫）

| 問題類型 | 判據 | 標準修法 |
|---|---|---|
| **A. 答非所問（誤命中）** | 查詢命中與話題無關的題 | 查命中題的 keywords 是否有泛化詞→加入停用詞表；或查詢子串撞上短 keyword→加長/移除 |
| **B. 缺專屬題** | 語料查詢無命中或命中相關但非最優 | 補一條專屬 Q&A（q 用真實話術，keywords 含繁簡雙版） |
| **C. keywords 缺口** | 題存在但查詢命中不了 | 補 keywords：口語化短語 + 簡體版 + 同義詞序（如 回吐/吐回去） |
| **D. 繁簡異體** | keyword 繁體（睡不著）查詢簡體（睡不着）不匹配 | T2S 不處理的異體（硬碟/硬盘、著/着）→ 直接補簡體 keyword |
| **E. 泛化 keyword** | keyword 被多題共用（≥5）且是語法詞 | 加入 KB_STOPWORDS（索引+計分雙重過濾） |
| **F. 匹配引擎 bug** | 短查詢命中不了長 keyword（奇數長度盲區） | n-gram len 步長改 1（已修） |
| **G. 分類優先級錯** | 書籍題搶產品題 | 書籍純備用（非書籍命中即返回，書籍只兜底）（已修） |
| **H. 情緒題被書籍搶** | 情緒話術命中書籍題 | 情緒優先層（matchBest<4 → matchEmotionFirst）+ 補情緒題 keywords |
| **I. prompt 層質量** | AI 模式回答不符合認知路徑 | 錯誤歸因四步 → 提煉成 prompt 規則 → 盲測回歸 |

---

## 四、迭代工作台用法

```bash
# 跑完整閉環（掃描 + 抽查 + 問題清單）
node kb_iterate.js
# 批量自我評審（答→評→重寫→盲比→沉澱）
node kb_iterate.js --self-review --n=20
# 認知能力測試（10 維度 37 題，測「該怎麼動作」）
node kb_iterate.js --cog

# 只看某類問題
node kb_iterate.js --scan        # 泛化 keyword / 繁簡異體掃描
node kb_iterate.js --audit       # 語料全量抽查（分類合理性）
node kb_iterate.js --report      # 出問題清單 + 修復建議（Markdown）

# 語料庫管理
node kb_iterate.js --add-queries "句1;句2"   # 追加真實用戶話術
```

---

## 五、語料庫（kb_queries.jsonl）

每條：`{"q": "真實話術", "expect": "期望命中分類或題", "source": "盲測/抽查/真實用戶", "ts": "日期"}`

| 來源 | 數量 | 覆蓋 |
|---|---|---|
| v31i 盲測 100 題 | 100 | 意圖識別/情緒/質疑/鏈 |
| 抽查 50 句 | 50 | 產品/交易/情緒/質疑/書籍 |
| 恐慌 8 題 | 8 | 極端情緒 |
| 攻擊質疑 18 題 | 18 | 四型質疑 |
| 真實用戶話術 | 持續追加 | 生產環境 |

---

## 六、迭代節奏建議

1. **每次知識庫/prompt 改動** → 跑 `node kb_iterate.js` 全量回歸
2. **每週** → 追加真實用戶話術到語料庫，重跑抽查
3. **每月** → 全量盲測 100 題 KPI 復測
4. **每次修復** → 先跑問題清單，修完再跑回歸，全過才 bump

---

## 七、與既有工具的關係

| 既有工具 | 角色 |
|---|---|
| v31_blind_test.js / v31i_judge.js | 盲測 KPI（機制④的深度驗證） |
| v32_attribution.js / v32b | 錯誤歸因（問題 I 的修法） |
| v35_chain*.js | 認知鏈驗證（prompt 層質量） |
| verify_stopwords.js / verify_sampling.js | **併入 kb_iterate.js**（②③） |
| check_backend_gap.js | 後台同步（機制⑤） |

kb_iterate.js 是**總調度**，把以上串成閉環，不取代它們。
