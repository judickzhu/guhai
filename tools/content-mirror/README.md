# 内容镜像（备份用，非生成器读取源）

本目录是 `电子书ipa/honglou_*.json`（生成器真实读取源）的**备份镜像**，用途只有一个：
**让提问者逐日积累的解码内容进入 git 版本管理，避免机器故障导致内容丢失。**

⚠️ 注意：
- 生成器 `build_honglou_site.py` 读的是 `../honglou_*.json`（仓库外），**不是**本目录。
- 修改内容请改仓库外那份，然后重新镜像到这里提交；两处不要各自编辑（会产生漂移）。
