# 《DC姐姐 V5｜方向探討》

> V4.x 收官後，DC姐姐 的下一階段往哪走？本文探討三個方向。
> 回顧：V3 會回答 → V4.0 認識同一個人 → V4.1 管理對話 → V4.2 跟著用戶走 → V4.3 行為驗收標準。
> 版本：V5 探討 · 2026-09

---

## 回顧：V4.x 達成了什麼

| 能力 | 達成 |
|---|---|
| 認識同一個人（跨輪記憶）| ✅ 200 輪 10/10 |
| 管理對話（債務/回退/循環/停得住）| ✅ 94.7% |
| 跟著用戶走（狀態機/群體模型/認知閉環）| ✅ 三方向全落地 |
| 行為驗收標準（100 輪狀態機規格）| ✅ 開發可直接用 |
| 後台對齊（轉達包/微調樣本）| ✅ 交付 |

**V4.x 完成的是「單用戶、單會話的深度」**——DC 對「一個用戶」能做到極致。

## 方向一：多用戶並行管理（從單用戶到多用戶）

**現狀**：V4.x 是「一個用戶 × 深度對話」。但真實場景是**多用戶並行**——DC 同時面對 10 個、100 個用戶。

**V5 目標**：多用戶上下文無串擾——
- 用戶 A 在談翻本、用戶 B 在談驗證——兩條線不混
- 群體模型（archetype）讓 DC 快速初始化新用戶（冷啟動）
- 認知存儲（DB）讓 DC 跨會話記住每個用戶

**驗證**：多用戶交替測試——A/B/C 三用戶交錯對話，DC 不串線、記得各自狀態。

## 方向二：主動型對話（從被動響應到主動出擊）

**現狀**：V4.x 是「用戶問 → DC 答」的被動模式。DC 很會接，但不會「主動給」。

**V5 目標**：主動型對話——
- 檢測用戶長時間未交易 → 主動提醒「最近沒開單，是行情沒機會還是在躲？」
- 用戶認知完成後的自然跟進 → 「上次你說要觀察一周，這周看到了什麼？」
- 跨會話的主動接續 → 「你上次卡在等待，這兩天行情剛好能拿來理解」

**這需要**：後台定時任務 + 認知存儲（知道每個用戶在哪、該推什麼）。

**驗證**：主動觸發場景測試——DC 在合適時機主動開口，而非永遠等用戶。

## 方向三：雙向價值閉環（從服務到陪伴成長）

**現狀**：DC 服務用戶，但用戶的「成長」沒有被追蹤。

**V5 目標**：**交易成長檔案**——
- 追蹤用戶認知階梯進展（L0→L5 走多遠）
- 記錄行為證明（哪些認知真正內化）
- 定期回顧：「三個月前你說扛不住，現在你連虧三天主動降倉了——這是進步」
- 把 DC 從「客服」變成「陪他走一段交易成長路的人」

**驗證**：長期用戶成長軌跡測試（跨月），DC 能指出用戶的實際變化。

---

## 建議優先序

1. **方向一（多用戶並行）**——真實業務剛需（後台就是多用戶），且已有基礎（DB 設計 + 群體模型）
2. **方向二（主動型）**——體驗升級（DC 從被動到主動），依賴後台定時任務
3. **方向三（成長檔案）**——長期願景（陪伴式），需較多產品設計

**共同前提**：後台認知存儲上線（V4.2 DB 設計）——三個方向都依賴「記住每個用戶」。

## 與後台對齊的銜接

- 方向一：DB 多用戶表（已有設計）+ archetype 冷啟動
- 方向二：後台定時任務 + cognitive_state 讀入
- 方向三：cognitive_nodes 成長追蹤（已有表）

**一句話**：V5 = 讓 DC 從「一個人的姐姐」變成「很多人的姐姐」——靠後台認知存儲（V4.2 已備）。
