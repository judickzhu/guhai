# 《DC姐姐 V4.2｜方向探討》

> V4.0（認識同一個人）+ V4.1（認知狀態機）已完成並驗證。本文探討 V4.2 的三個可能方向。
> 版本：V4.2 探討 · 2026-09

---

## 回顧：DC姐姐 能力進化

| 版本 | 能力 | 驗證 |
|---|---|---|
| V3.x | 會回答（五問/八意圖/銷售狀態機）| 盲測 9.31 |
| V4.0 | 會認識同一個人（心理軌跡/認知畫像/弦外之音）| 20 人格碰撞 + 200 輪 10/10 |
| V4.1 | 會管對話（認知債務/回退/遷移/循環斷路/反銷售）| 10×10 9/10 + 專項 94.7% + 反銷售 5/5 |

## 方向一：多用戶記憶（跨會話認知檔案持久化）

**現狀**：V4.0/V4.1 的認知狀態是「會話內」的——同一次對話記得用戶，但**跨會話不持久**（用戶明天再來，DC 忘了他昨天懂到哪）。

**V4.2 目標**：認知檔案**持久化到後台 DB**，跨會話帶入：
```
用戶 U001 第 1 次對話（9/12）
  認知層 L2、卡點「等待=錯過」、關係 R3、已理解 ①②、未理解 ③
第 2 次對話（9/15）
  自動帶入：你上次卡在「為什麼等待也是交易」，這次我們接著聊？
```
**這需要後台 DB 支援**（認知狀態存儲），是需求⑩ 的延伸。

**驗證**：跨會話測試——同一用戶隔 N 天再來，DC 能否續接上次認知（不重新認識）？

## 方向二：群體用戶模型（從個案到規律）

**現狀**：每個用戶當個案處理。

**V4.2 目標**：**從 20 人格模型（P01-P20）+ 10 測試用戶（U01-U10）沉澱「群體規律」**：
- 翻本型用戶的第 3 次翻本請求 → 直接觸發 LOOP_BREAK（不必等到繞 3 圈才發現）
- 新手恐懼型的「虧光本金」提問 → 先接恐懼再給最小步驟（模式化）
- 用已驗證的 200 輪測試數據，歸納「每種人格的典型軌跡 + 最優響應模板」

**這讓 DC 從「每個用戶重新學」變成「帶著規律認識新用戶」**——冷啟動更快。

**驗證**：新人格（P21+）預測測試——用規律預測其軌跡，對比實際對話。

## 方向三：行為證明閉環（認知完成自動檢測）

**現狀**：行為證明（用戶下一句能否用上認知）靠人工/評審判斷。

**V4.2 目標**：**自動化認知完成檢測器**：
- 維護「本輪推進的認知節點」
- 監測用戶後續 N 輪是否「使用該認知」（提到/應用/不重犯）
- 命中 → 認知完成 ✅（更新狀態）；未命中 → 認知債務未清（回退預警）

**這把 V4.1 的「行為證明」從概念變成自動化機制**，是後台診斷層的核心件。

**驗證**：用 20 題專項測試的歷史數據校準檢測器（哪些回答算「用了認知」）。

---

## 建議優先序

1. **方向一（多用戶記憶）**——最接近「像真人」的本質（記得用戶，跨會話），且與需求⑩ 後台集成天然對接
2. **方向三（行為證明自動化）**——把 V4.1 核心概念落地為後台機制，可自動迭代
3. **方向二（群體模型）**——錦上添花，數據量夠時自然湧現

**共同前提**：需要後台 DB 支援（認知狀態存儲）+ LLM 額度（測試驗證）——建議先解決這兩個基礎設施。

---

## 附：當前待辦（受 key 額度阻塞）

- [ ] G03 修復驗證（規則已寫，待 key 恢復）
- [ ] 全量回歸（v81 三規則無副作用，需重建 runner + key）
- [ ] 反銷售檢測器全量樣本測試（後台集成時）
